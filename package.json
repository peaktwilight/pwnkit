{
  "name": "nightfang",
  "type": "module",
  "version": "0.1.1",
  "description": "AI-powered agentic security scanner. Scan endpoints, audit packages, review source code. Four autonomous agents discover, attack, verify, and report.",
  "bin": {
    "nightfang": "dist/index.js"
  },
  "files": [
    "LICENSE",
    "README.md",
    "package.json",
    "dist"
  ],
  "scripts": {
    "build": "pnpm -r build && pnpm run build:bundle",
    "build:bundle": "node ./scripts/bundle-cli.mjs",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r exec tsc --noEmit",
    "test": "pnpm --filter @nightfang/test-targets test",
    "clean": "pnpm -r clean",
    "vulnerable": "PORT=43100 pnpm --filter @nightfang/test-targets vulnerable",
    "safe": "PORT=43101 pnpm --filter @nightfang/test-targets safe"
  },
  "keywords": [
    "ai",
    "application-security",
    "mcp",
    "owasp",
    "security",
    "red-team",
    "llm",
    "prompt-injection"
  ],
  "author": "Peak Twilight",
  "homepage": "https://nightfang.dev",
  "bugs": {
    "url": "https://github.com/peaktwilight/nightfang/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/peaktwilight/nightfang.git"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20",
    "pnpm": ">=8"
  },
  "dependencies": {
    "@clack/prompts": "^1.1.0",
    "@nightfang/test-targets": "workspace:*",
    "better-sqlite3": "^12.8.0",
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "drizzle-orm": "^0.44.0",
    "ora": "^8.0.0",
    "yaml": "^2.4.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "esbuild": "^0.27.4"
  },
  "pnpm": {
    "overrides": {
      "esbuild": "^0.27.4"
    }
  }
}
